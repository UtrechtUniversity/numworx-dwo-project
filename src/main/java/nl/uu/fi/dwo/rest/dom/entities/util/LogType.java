package nl.uu.fi.dwo.rest.dom.entities.util;

/**
 * Type of JSON stored into PersistentLogData. It the enum labels should
 * be equal to the java class types.
 * 
 * @author Gert van der Plas
 */
public enum LogType {
    JSONObject    //a flexible class, fixed classes can be Persistent entities for example.
}
