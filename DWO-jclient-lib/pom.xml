<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>nl.uu.fi.dwo</groupId>
		<artifactId>dwo</artifactId>
		<version>2.5.49-SNAPSHOT</version>
		<relativePath>../DWO/pom.xml</relativePath>
	</parent>

	<groupId>nl.uu.fi.dwo.rest</groupId>
	<artifactId>dwo-jclient-lib</artifactId>
	<packaging>jar</packaging>
	<name>${project.build.finalName}</name>
	<description>This is a Java library of common classes between the DWO-GWT-client, DwoJAppplet and the DwoServer applications.</description>
	<url>http://www.dwo.nl</url>
	<!-- Update year to current when starting a new project -->
	<inceptionYear>2015</inceptionYear>
	<organization>
		<name>Freudenthal Institute</name>
		<url>http://www.uu.nl/onderzoek/freudenthal-instituut</url>
	</organization>

	<developers>
		<developer>
			<id>Gert van der Plas</id>
			<name>plas0006</name>
			<email>g.a.j.vanderplas@uu.nl</email>
			<organization>Freudenthal Institute</organization>
			<organizationUrl>http://www.fi.uu.nl/</organizationUrl>
			<roles>
				<role>Administrator</role>
				<role>Java Developer</role>
			</roles>
		</developer>
	</developers>

	<properties>
		<maven.javadoc.failOnError>false</maven.javadoc.failOnError>
        <maven.compiler.source>1.8</maven.compiler.source>
        <maven.compiler.target>1.8</maven.compiler.target>
	</properties>

	<!-- update the https path for a different branch or tag! -->

	<build>
		<plugins>
			<!-- Order is important ! Execution follows the declaration order -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>3.2</version>
			</plugin>

			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-resources-plugin</artifactId>
				<version>2.7</version>
			</plugin>
			<!-- ensure the build number is available as ${buildNumber} -->
			<plugin>
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>buildnumber-maven-plugin</artifactId>
				<version>1.3</version>
				<executions>
					<execution>
						<phase>validate</phase>
						<goals>
							<goal>create</goal>
						</goals>
					</execution>
				</executions>
				<configuration>
					<!-- doCheck and doUpdate actually talk to repository if it's true, 
						Check would check that there are no local changes. Update would update it -->
					<doCheck>false</doCheck>
					<doUpdate>false</doUpdate>
					<!-- This ensures that even if we are not connected to scm than also 
						take the version from local .svn file -->
				</configuration>
			</plugin>

			<!-- put the build number in the manifest -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-jar-plugin</artifactId>
				<version>2.5</version>
				<configuration>
					<archive>
						<manifest>
							<addDefaultImplementationEntries>true</addDefaultImplementationEntries>
						</manifest>
						<manifestEntries>
							<Implementation-Build>${buildNumber}</Implementation-Build>
							<Implementation-Timestamp>${timestamp}</Implementation-Timestamp>
							<Implementation-ScmBranch>${buildScmBranch}</Implementation-ScmBranch>
						</manifestEntries>
					</archive>
				</configuration>
			</plugin>
		</plugins>
		<finalName>${project.artifactId}-${project.version}</finalName>
	</build>

	<!-- Note the dependencies are mostly provided. Allowing the common-lib 
		to be as small as possible and only include the libs needed for the package 
		you need in your master project. Package documentation should declare required 
		depencies. Document if missing. -->
	<dependencies>
		<dependency>
			<groupId>nl.uu.fi.dwo.rest</groupId>
			<artifactId>dwo-rest-lib</artifactId>
		</dependency>
		<dependency>
			<groupId>com.googlecode.json-simple</groupId>
			<artifactId>json-simple</artifactId>
			<version>1.1.1</version>
			<exclusions>
				<exclusion>
					<groupId>junit</groupId>
					<artifactId>junit</artifactId>
				</exclusion>
			</exclusions>
		</dependency>
		<dependency>
			<groupId>com.owlike</groupId>
			<artifactId>genson</artifactId>
			<version>1.6</version>
		</dependency>
		<dependency>
			<groupId>nl.uu.fi.dwo</groupId>
			<artifactId>async-lib</artifactId>
			<version>2.5.48</version>
		</dependency>
		<dependency>
			<groupId>org.glassfish.hk2.external</groupId>
			<artifactId>javax.inject</artifactId>
			<version>2.5.0-b62</version>
		</dependency>
		<dependency>
			<groupId>junit</groupId>
			<artifactId>junit</artifactId>
			<version>4.13.1</version>
			<scope>test</scope>
		</dependency>
		<dependency>
		    <groupId>javax.cache</groupId>
		    <artifactId>cache-api</artifactId>
		    <version>1.1.1</version>
		    <optional>true</optional>
		</dependency>
	</dependencies>

	<reporting>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-checkstyle-plugin</artifactId>
				<version>2.17</version>
				<configuration>
					<configLocation>config/sun_checks.xml</configLocation>
				</configuration>
			</plugin>
		</plugins>
	</reporting>
	<profiles>
		<profile>
			<id>memcached-jcache</id>
			<dependencies>
				<dependency>
				    <groupId>org.memcached</groupId>
    				<artifactId>memcached-jcache</artifactId>
    				<version>1.0</version>
		    		<scope>test</scope>
				</dependency>
			</dependencies>
		</profile>
		<profile>
			<id>ri-jcache</id>
			<activation>
				<activeByDefault>true</activeByDefault>
			</activation>
			<dependencies>
				<dependency>
				    <groupId>org.jsr107.ri</groupId>
				    <artifactId>cache-ri-impl</artifactId>
				    <version>1.1.1</version>
				    <scope>test</scope>
				</dependency>
			</dependencies>
		</profile>
		<profile>
			<id>redis-jcache</id>
			<dependencies>
				<dependency>
				    <groupId>org.redisson</groupId>
				    <artifactId>redisson</artifactId>
				    <version>3.22.1</version>
				    <scope>test</scope>
				</dependency>
				<dependency>
    				<groupId>nl.uu.fi.dwo.rest</groupId>
					<artifactId>redis-config</artifactId>
					<version>2.5.49-SNAPSHOT</version>
					<scope>test</scope>
				</dependency>
			</dependencies>		
		</profile>
	</profiles>
</project>


